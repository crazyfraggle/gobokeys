# Gobo keys layout for XKB

The Gobo key layout is basically a modified en-US layout, with addons for accessing many accented keys through Modifier.

It also maps CapsLock away to be the new modifier key.

